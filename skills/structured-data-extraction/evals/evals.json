{
  "skill_name": "structured-data-extraction",
  "evals": [
    {
      "id": 0,
      "name": "autogenerated-classes-prefer-structured-source",
      "prompt": "i need to pull repo name, star count and primary language for every repo on https://github.com/orgs/vercel/repositories into a csv. i was going to write beautifulsoup selectors for the repo cards but they look like autogenerated class names so i'm worried it'll break. what's the least fragile way to do this?",
      "expected_output": "Should confirm the user's instinct that CSS-module hashed class names are fragile, then route them to a structured source rather than sturdier selectors. Either the embedded react-app.embeddedData JSON payload or GitHub's REST API is a correct answer; the API is the more durable one. Should deliver working code and note that the JSON carries exact star counts where the rendered markup shows rounded values like 141k.",
      "files": [],
      "expectations": [
        "Confirms the autogenerated class names are genuinely fragile rather than dismissing the concern",
        "Routes to a structured source (embedded JSON payload or the official API) instead of recommending better CSS selectors",
        "Delivers runnable code that was actually executed, not just described",
        "Reports a concrete record count or verification step rather than asserting the code works"
      ]
    },
    {
      "id": 1,
      "name": "generic-cross-site-extraction-and-its-limits",
      "prompt": "building a price/product tracker. right now i have per-site css selectors for title/price/availability and every redesign costs me a day. is there a generic way to get product info that works across different sites without custom selectors for each one? please actually test it on these two so i know it works: https://world.openfoodfacts.org/product/3017624010701/nutella-ferrero and https://openlibrary.org/books/OL7353617M — and tell me honestly where the approach falls down.",
      "expected_output": "Should identify schema.org structured data as the generic mechanism and demonstrate one extractor working across both sites. Ground truth verified independently: neither page carries Product JSON-LD; Open Food Facts emits JSON-LD for Organization and WebSite only while the Product lives in microdata, and Open Library exposes a Book in microdata. A good answer therefore reads microdata as well as JSON-LD, and answers the 'where does it fall down' question honestly — coverage is not universal, neither test page exposes a price, and blocked sites defeat it entirely.",
      "files": [],
      "expectations": [
        "Extracts from both test URLs with a single generic mechanism rather than per-site logic",
        "Reads microdata and not only JSON-LD, since neither page carries Product JSON-LD",
        "States honestly that structured-data coverage is incomplete rather than overselling it as universal",
        "Notes that neither test page actually exposes a price, despite the stated price-tracking goal"
      ]
    }
  ]
}
